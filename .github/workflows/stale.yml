# Disabled for private fork — stale issue management is not relevant here.
name: Stalebot
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: "true"
